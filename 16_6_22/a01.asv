% Pies sin dice
pie = @(a, b, a_b) a + b - a_b;

a = .1;
b = .2;
a_b = .05;

disp(pie(a, b, a_b))

% white and black
with_rem = @(b, w) factorial(w) / (factorial(1)*factorial(w-1)) * ...
    factorial(b) / (factorial(1)*factorial(b-1)) / ...
    (factorial(w+b) / (factorial(2)*factorial(w+b-1)));

b = 4;
w = 3;
